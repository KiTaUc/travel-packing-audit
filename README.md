# Travel Packing Audit

Сверяет список вещей поездки и показывает, что ещё не упаковано по категориям.

## Запуск

```bash
python src/travel_packing_audit.py missing examples/sample.json
```

Локальный инструмент без внешних сервисов. Проверка: `python -m unittest discover -s tests -v`.
